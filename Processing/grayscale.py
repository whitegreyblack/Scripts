import PIL
import sys

def grayscale():
    # open img file read
    # open img file write
    pass

if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("not enough arguments")
        exit(-1)
    else:
        grayscale()
